<UserControl x:Class="SmartPhotShop.Views.FieldsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartPhotShop.Views"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:cal="http://caliburnmicro.com"
             mah:DialogParticipation.Register="{Binding}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Content="New Field"
                    Margin="4"
                    Width="120"
                    x:Name="NewField"/>

            <Button Content="Import Columns"
                    Margin="4"
                    Width="120"
                    x:Name="ImportColumns"/>

            <Button Content="Export Columns"
                    Margin="4"
                    Width="120"
                    x:Name="ExportColumns"/>
            <Button Content="Save Changes"
                    Margin="4"
                    Width="120"
                    x:Name="SaveChanges"/>
        </StackPanel>
        <DataGrid x:Name="FieldsCollectionView"
                  AutoGenerateColumns="False"
                  Grid.Row="1"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Group" Binding="{Binding Group}"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                <DataGridTextColumn Header="Order" Binding="{Binding Order}"/>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Delete"
                                        cal:Message.Attach="DeleteField($dataContext)"/>
                                
                                <Button Content="Move Up"
                                        cal:Message.Attach="MoveUp($dataContext)"/>

                                <Button Content="Move Down"
                                        cal:Message.Attach="MoveDown($dataContext)"/>

                            </StackPanel>
                          
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
