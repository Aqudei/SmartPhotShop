<UserControl x:Class="SmartPhotShop.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartPhotShop.Views"
             xmlns:ico="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mah:DialogParticipation.Register="{Binding}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding ProductTemplates}"
                  FontSize="20"
                  CellEditEnding="DataGrid_CellEditEnding"
                  RowHeight="60"
                  Grid.Row="0">
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="2">
            <Button x:Name="Save" Margin="5" Padding="10,5">
                <StackPanel Orientation="Horizontal">
                    <ico:PackIconBootstrapIcons Kind="Floppy" VerticalAlignment="Center"/>
                    <Label>SAVE CHANGES</Label>
                </StackPanel>
            </Button>
            
            
        </StackPanel>
    </Grid>
</UserControl>
