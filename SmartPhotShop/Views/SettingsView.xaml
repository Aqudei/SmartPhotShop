<UserControl x:Class="SmartPhotShop.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartPhotShop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Working Directory:" VerticalAlignment="Center" Width="120"/>
                <TextBox x:Name="WorkingDirectory" 
                         Width="350" Margin="5,0,0,0"/>
                <Button Content="Browse..." Width="75" Margin="5,0,0,0" x:Name="BrowseWorkingDirectory"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="32,0,0,10">
                <TextBlock Text="Error Directory:" VerticalAlignment="Center" Width="120"/>
                <TextBox x:Name="ErrorDirectory" Width="400" Margin="5,0,0,0" IsReadOnly="True"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="32,0,0,10">
                <TextBlock Text="Done Directory:" VerticalAlignment="Center" Width="120"/>
                <TextBox x:Name="DoneDirectory" Width="400" Margin="5,0,0,0" IsReadOnly="True"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="32,0,0,10">
                <TextBlock Text="Output Directory:" VerticalAlignment="Center" Width="120"/>
                <TextBox x:Name="OutputDirectory" Width="400" Margin="5,0,0,0" IsReadOnly="True"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center" Margin="0,32,0,0">
                <Button Content="Save" 
                        x:Name="Save"
                        Width="80"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
