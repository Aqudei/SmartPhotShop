<UserControl x:Class="SmartPhotShop.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartPhotShop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Working Directory -->
        <TextBlock Text="Working Directory:" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0"/>
        <TextBox x:Name="WorkingDirectory" Margin="4,4,4,4" Grid.Row="0" Grid.Column="1"/>
        <Button Content="Browse..." Width="75" Margin="4,4,4,4" x:Name="BrowseWorkingDirectory" Grid.Row="0" Grid.Column="2"/>

        <!-- Error Directory -->
        <TextBlock Text="Error Directory:" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0"/>
        <TextBox x:Name="ErrorDirectory" IsReadOnly="True" Grid.Row="1" Grid.Column="1" Margin="4,4,4,4"/>

        <!-- Done Directory -->
        <TextBlock Text="Done Directory:" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0"/>
        <TextBox x:Name="DoneDirectory" IsReadOnly="True" Grid.Row="2" Grid.Column="1" Margin="4,4,4,4"/>

        <!-- Output Directory -->
        <TextBlock Text="Output Directory:" VerticalAlignment="Center" Grid.Row="3" Grid.Column="0"/>
        <TextBox x:Name="OutputDirectory" IsReadOnly="True" Grid.Row="3" Grid.Column="1" Margin="4,4,4,4"/>

        <!-- Flat File -->
        <TextBlock Text="Flat File:" VerticalAlignment="Center" Grid.Row="4" Grid.Column="0"/>
        <TextBox x:Name="FlatFile" IsReadOnly="True" Grid.Row="4" Grid.Column="1" Margin="4,4,4,4"/>
        <Button Content="Browse..." Width="75" Margin="4,4,4,4" x:Name="BrowseFlatFile" Grid.Row="4" Grid.Column="2"/>

        <!-- Save Button -->
        <Button Content="Save" x:Name="Save" Width="80" Grid.Row="5" Grid.Column="1" Margin="0,20,0,0" HorizontalAlignment="Center"/>
    </Grid>

</UserControl>
